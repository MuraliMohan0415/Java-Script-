<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <script>
        document.write("<hr>")
        // let a = parseInt(prompt("Enter a number : "))
        // if(a%2==0 && a%3==0) {
        //     document.write(`Number ${a} is divisble by 2 & 3`)

        // }else{
        //     document.write(`Number ${a} is not divisble by 2 & 3`)
        // }
        // document.write("<hr>")

        // let a =parseInt(prompt("Enter first number : "))
        // let b =parseInt(prompt("Enter second number : "))
        // let c =parseInt(prompt("Enter third number : "))
        // if(a>b && a>c){
        //     document.write(`First number ${a} is largest among ${b} and ${c}`)
        // }else if(b>a && b>c){
        //     document.write(`second number ${b} is largest among ${a} and ${c}`)
        // }else{
        //     document.write(`Third number ${c} is largest among ${a} and ${b}`)
        // }  
         // document.write("<hr>")

        let a =parseInt(prompt("Enter the number of working days: "))
        let b =parseInt(prompt("Enter the number of days for absent: "))
        let c = ((a-b)/a)*100
        if(c>75){
            document.write(`You are eligible to write exam for the attendance percentage :${c}`)
        }else{
            document.write(`You are not eligible to write exam for the attendance percentage:  ${c}`)
        }
        
    </script>
</body>
</html>
